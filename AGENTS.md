# CTFLab 项目约束

本项目是面向 Apple Silicon Mac 的本地 QEMU 靶场运行器。主要实现位于 `tools/`，回归测试位于 `tools/tests/`。

## 资产与网络边界

- 原始镜像保持只读，转换和来宾修改写入可恢复副本或 QCOW2 overlay。
- 脆弱靶机默认留在回环隔离网络。接入物理局域网、增加对外监听或扩大测试目标范围须有对应授权。
- 在线元数据只生成白名单候选配置，不传入任意 QEMU 参数；不得把推断结果标为已验证事实。
- 遵守 `.gitignore` 的入库白名单。虚拟磁盘、凭据、运行日志、课程题库和成绩数据不提交。
- 保留现有用户改动；修改实现时维持导入、启动、停止、重置的并发保护和可恢复性。

## 按任务选择资料与验证

- 使用和环境问题查 `README.md`、`docs/ctflab-phase1-quickstart.md`；架构调整查 `docs/ctflab-mac-mvp-implementation-plan.md`；历史验收查 `docs/verification-2026-09-06.md`。只读相关部分，历史记录不替代当前验证。
- 根据变更选择受影响测试。完整本地回归入口：`python3 -m unittest discover -s tools/tests -v`。语法检查入口见 README。
- 修改固件、磁盘、网络或生命周期行为时验证对应路径；需要启动或重置虚拟机时使用授权的测试实例和派生磁盘。
- 只有交付“已适配、可用”的镜像时，才要求完整冷启动、显示或登录、网络、重启及目标服务验证。诊断、候选配置或仅格式转换按实际完成状态交付。
- 交付说明行为变化、已运行的验证及未验证项，不将截图中的 UEFI Shell 或错误画面视为成功启动。
